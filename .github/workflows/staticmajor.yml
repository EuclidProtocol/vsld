# Staticmajor: Static analyzer to catch leaking resources & other bad code patterns
# Disabled: orijtech/staticmajor-action bundles Go 1.21.4 in its Docker image
# and cannot work with go.mod requiring >= 1.23.2. The action has not been
# updated since 2022. Re-enable if upstream publishes a newer image.
#
# name: Detect leaking resources and bad code patterns
# on:
#   pull_request:
#     paths:
#       - "**.go"
#   push:
#     branches: [ main ]
#     paths:
#       - "**.go"
#
# jobs:
#   run_staticmajor:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Check out repository code
#         uses: actions/checkout@v5
#       - name: Staticmajor action
#         id: staticmajor
#         uses: orijtech/staticmajor-action@main
#         with:
#           packages: ./...
#           resleak: true
#           structslop: false
#           tests: false
